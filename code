#include <bits/stdc++.h>

using namespace std;

int main()
{
    int n, i = 1, a;
    
    cin >> n;
    
    for(i = 1; i <= n; i ++)
    {
        cin >> a;
        
        if(n < 0)
        {
            n = n * (-1);
        }
        
        float k = cbrt(a);
        
        if(k == (int) k)
        {
            cout << "DA" << '\n';
        }
        else
        {
            cout << "NU" << '\n';   
        }
    }

    return 0;
}
